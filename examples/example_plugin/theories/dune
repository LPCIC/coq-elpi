(rocq.theory
  ; myExtension is the logical path that can be used in rocq to import the
  ; the .v in the theories folder
 (name myExtension)
  ; these are the dependencies of the current package
 (theories elpi_elpi elpi myExtension.lib)
  ; the rocq files in this folder (theories)
  ; will use the pluging called ext.plug
  ; the rocq files in this folder use the pluging defined in src/
 (plugins myExtension.plugin)
 (package myExtension))

(include_subdirs qualified)
